version: 0.1
frontend:
  phases:
    build:
      commands:
        - npm run build
    artifacts:
      baseDirectory: public
      files:
        - '**/*'
    cache:
      paths:
        - public/**/*
