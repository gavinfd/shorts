version: 0.2
frontend:
  phases:
    build:
      commands:
        - npm run build
    artifacts:
      baseDirectory: public
      files:
        - '**/*'
    cache:
      paths:
        - public/**/*
