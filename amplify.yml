version: 0.1
frontend:
  phases:
    build:
      commands:
        - yarn build
    artifacts:
      baseDirectory: public
      files:
        - '**/*'
    cache:
      paths:
        - public/**/*
